# Hook tests

Mirror of `src/hooks/`. Add tests here with the same path structure.

Examples:

- `tests/hooks/useBackgroundIdeaUndoRedo.test.ts`
- `tests/hooks/useBackgroundSheet.test.ts`

Use `@/hooks/...` or `@/lib/...` for imports (resolves to `src/`).
