# Component tests

Mirror of `src/components/`. Add tests here with the same path structure.

Examples:

- `tests/components/Backgrounds/BackgroundGrid.test.tsx`
- `tests/components/BackgroundSheet/BackgroundIdeaSection.test.tsx`

Use `@/components/...` for imports (resolves to `src/components/`).
